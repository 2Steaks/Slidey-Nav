# Graphite-Nav-JS
jQuery plugin for auto centering mobile menu (currently requires GSAP TweenMax)

Includes:
- Dragging
- Scrolling
- Edge detection

```js
$('nav').graphiteNav({
    active_class: 'is--active'
});
```